# Confluent Cloud API Credentials
# Copy this file to .env and fill in your actual credentials
# Then run: source .env

export CONFLUENT_CLOUD_API_KEY="your-api-key-here"
export CONFLUENT_CLOUD_API_SECRET="your-api-secret-here"

# Claude Backend Configuration
# Choose 'anthropic' for direct Anthropic API or 'bedrock' for AWS Bedrock
export CLAUDE_BACKEND="bedrock"  # Options: 'anthropic' or 'bedrock'

# Option 1: Anthropic API (direct)
# Get your API key from: https://console.anthropic.com/
export ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Option 2: AWS Bedrock (recommended if you have AWS credits)
# Requires AWS credentials with bedrock:InvokeModel permissions
# AWS credentials can be set via:
# - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# - AWS credentials file (~/.aws/credentials)
# - IAM role (if running on AWS)
export AWS_REGION="us-east-1"  # Region where Bedrock is enabled
export AWS_ACCESS_KEY_ID="your-aws-access-key-id"
export AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
