# Confluent Cloud API Credentials (for Terraform)
# These are ORGANIZATION-LEVEL Cloud API Keys (not cluster-specific keys)
# Get from: Confluent Cloud Console → Cloud API Keys
# Used by: Terraform for infrastructure provisioning
#
# Copy this file to .env and fill in your actual credentials
# Then run: source .env

export CONFLUENT_CLOUD_API_KEY="your-api-key-here"
export CONFLUENT_CLOUD_API_SECRET="your-api-secret-here"
